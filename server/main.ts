import './imports/methods/channels.method';
// import './imports/methods/feeds.method';
import './imports/methods/episodes.method';
import './imports/publications/episodes'

import { Meteor } from 'meteor/meteor';

// import { Episodes } from '../imports/collections/episodes';


Meteor.startup( () => {
  // code to run on server at startup
  // if ( Episodes.find( {} ).cursor.count() === 0 ) {

  //   let episodeId = Episodes.collection.insert( {
  //     title: 'Ethan Gonzalez',
  //   } );
  // }
} );
